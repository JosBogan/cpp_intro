#include <iostream>
#include "pointers_and_references.cpp"
#include "data_types.cpp"
#include "functions.cpp"

int main()
{

  // pointers_and_references(); // ? POINTERS AND REFERENCES

  // arrays(); // ? ARRAYS

  // add(); // ? FUNCTION PROTOTYPES

  static_function(); // ? STATIC VARIABLE SCOPE

  // strings(); // ? STRING

  return 0;
}